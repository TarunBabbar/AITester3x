[project]
name = "jira-qa-crew-vercel"
version = "1.0.0"
description = "Jira QA Crew pipeline on Vercel (Flask)"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.vercel]
entrypoint = "api.index:app"
